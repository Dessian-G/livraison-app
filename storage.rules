rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Images produits et logo : lecture publique, écriture authentifiée (admin)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
